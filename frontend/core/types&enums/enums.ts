export enum AxiosMethods {
  POST = "post",
  GET = "get",
  PUT = "put",
  DELETE = "delete",
  HEAD = "head",
}

export enum LanguageEnum {
  AR = "ar",
  EN = "en",
}

export enum DirectionEnum {
  RTL = "rtl",
  LTR = "ltr",
}

export enum ThemeEnum {
  LIGHT = "LIGHT",
  DARK = "DARK",
}
